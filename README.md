#### **CompliantCompiler CC.**
##### **Copylefter, Compiler, Compliant.**
```cpp
#include <identity>
int main() {
    std::CompliantCompiler_CC << "C/C++" << "Copylefter";
    return 0;
}
```
I run Arch and Gentoo because I want my OS to stay out of my way. I don't care what you run; I care if your code is readable.
